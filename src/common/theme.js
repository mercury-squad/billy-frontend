import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#4b4ded',
    },
    secondary: {
      main: '#68d9cf',
    },
  },
});

export default theme;
